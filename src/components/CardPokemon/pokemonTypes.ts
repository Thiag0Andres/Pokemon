import {mvs} from 'react-native-size-matters';
import styled from 'styled-components/native';

export const Grass = styled.Text`
  justify-content: center;
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: ${({theme}) => theme.colors.green};
  margin: 0 ${mvs(15)}px;
`;

export const Ground = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #e2c569;
  margin: 0 ${mvs(15)}px;
`;

export const Fire = styled.Text`
  justify-content: center;
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #f08030;
  margin: 0 ${mvs(15)}px;
`;

export const Water = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #6890f0;
  margin: 0 ${mvs(15)}px;
`;

export const Fighting = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #c03028;
  margin: 0 ${mvs(15)}px;
`;

export const Ice = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #98d8d8;
  margin: 0 ${mvs(15)}px;
`;

export const Poison = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #a040a0;
  margin: 0 ${mvs(15)}px;
`;

export const Dark = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #705848;
  margin: 0 ${mvs(15)}px;
`;

export const Fairy = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #f7b5f7;
  margin: 0 ${mvs(15)}px;
`;

export const Psychic = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #df5f83;
  margin: 0 ${mvs(15)}px;
`;
export const Normal = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #a8a878;
  margin: 0 ${mvs(15)}px;
`;

export const Ghost = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #705898;
  margin: 0 ${mvs(15)}px;
`;
export const Rock = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #b8a038;
  margin: 0 ${mvs(15)}px;
`;

export const Dragon = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #7860e2;
  margin: 0 ${mvs(15)}px;
`;

export const Flying = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #8d9fee;
  margin: 0 ${mvs(15)}px;
`;

export const Steel = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #b1b1c0;
  margin: 0 ${mvs(15)}px;
`;

export const Electric = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #fce134;
  margin: 0 ${mvs(15)}px;
`;

export const Bug = styled.Text`
  color: ${({theme}) => theme.colors.white};
  font-weight: 600;
  text-shadow: ${mvs(2)}px ${mvs(2)}px #000;
  padding: ${mvs(3)}px ${mvs(10)}px;
  border-radius: ${mvs(5)}px;
  background-color: #a4b31c;
  margin: 0 ${mvs(15)}px;
`;
